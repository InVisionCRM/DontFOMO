# CLAUDE.md — cryptolife (the app)

The project's standing brief lives one level up. Read it first — it is
binding.

@../CLAUDE.md

---

## App-folder notes

- This folder is the Expo / React Native app. The design documents and
  the master `CLAUDE.md` are in the parent `/CryptoLife/` directory.
- **Expo SDK 54 / React Native 0.81.** Expo changes between versions —
  before writing native or Expo-API code, check the exact versioned
  docs at https://docs.expo.dev/versions/v54.0.0/.
- Architecture, coding standards, the theme system and the definition
  of done are all in the parent `CLAUDE.md`. Do not duplicate them here.
- Update `CHANGELOG.md` (in this folder) after any coding work.

/**
 * src/data — static, data-driven game content.
 * ------------------------------------------------------------------
 * Tokens, scam events, milestones and skill-tree nodes live here as
 * DATA, not code. New content should mean a new data entry, not new
 * logic. See CLAUDE.md §5.
 *
 * Empty for now — filled as each system comes online.
 */
export {};

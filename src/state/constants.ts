/**
 * constants.ts — shared store constants (no store imports).
 */

/** Starting cash for a fresh game. */
export const STARTING_CASH = 500;

/** Placeholder handle until onboarding completes. */
export const DEFAULT_HANDLE = '@new_player';
